{
  "name": "webnode",
  "version": "1.0.0",
  "description": "Ejemplo de usar express",
  "main": "server.js",
  "scripts": {
    "check:node": "check-node-version --package",
    "prestart": "npm run check:node",
    "start": "gulp",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "author": "Duivan",
  "license": "ISC",
  "engines": {
    "node": ">=10.15 <12.0"
  },
  "dependencies": {
    "cors": "^2.8.4",
    "express": "^4.16.3",
    "morgan": "^1.9.0"
  },
  "devDependencies": {
    "check-node-version": "^3.3.0",
    "gulp": "^4.0.0",
    "gulp-nodemon": "^2.4.1"
  }
}
